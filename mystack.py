#new() -creates new stack
# push() adds an object onto the stack
#
# pop() removes the top item from the stack
# top() returns the top item from the statck without removing it
#
# size() returns the number of objects in the stack
# isEmpty indicates if the stack is empty
from pythonds.basic.stack import Stack

for(i = stocks.length-1; i >= 0; i --){
    for(j=i; j<= 0; j--){
        if(stocks[i] >= stocks [j]) {
            spans[i] += 1
        }
    }
}
